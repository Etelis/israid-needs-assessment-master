const questionBox = {
  margin: '15px 8px',
  textAlign: 'center',
  borderRadius: '5px',
  border: '1px solid gray',
};

const notesBox = {
  padding: 2,
  '& label': {
    py: 2,
    px: '21px',
  },
};

const styles = { questionBox, notesBox };

export default styles;
