{
    "type": "object",
    "properties": {
        "question": {
            "type": "string",
            "description": "The question to search the answer to"
        },
        "context": {
            "type": "array",
            "items": {
                "type": "string"
            },
            "description": "An array of paths in which to search the answer"
        }
    },
    "required": [
        "question",
        "context"
    ]
}