const defaultProgressCard = {
  width: '100%',
  borderRadius: 'px',
};

const progressCardContent = {
  '&:last-child': {
    paddingBottom: '16px',
  },
};

const lastUpdatedText = {
  color: 'Grey',
};

const styles = { defaultProgressCard, progressCardContent, lastUpdatedText };

export default styles;
