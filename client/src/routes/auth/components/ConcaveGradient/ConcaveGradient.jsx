import Box from '@mui/material/Box';
import styles from './styles'; 
//TODO: Fix the styles to llok like in figma  -this is the orange shap at the bottom of login
const ConcaveGradient = () => {
  return (
    <Box sx={styles.container}></Box>
  );
};

export default ConcaveGradient;
