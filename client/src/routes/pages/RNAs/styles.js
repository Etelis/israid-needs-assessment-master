const rnasPage = {
  width: '100%',
  marginTop: 2,
};

const rnasList = {
  height: '50vh',
  overflow: 'auto',
  width: '100%',
};

const rna = {
  width: '100%',
  marginY: '1vh',
};

const newRnaButton = {
  width: '75%',
};

const styles = { rnasPage, rnasList, rna, newRnaButton};

export default styles;
