const rnasPage = {
	width: '100%',
	height: '90vh',
	padding: '14px',
	paddingBottom: 2,
	paddingTop: 0,
	alignItems: 'center',
	flexGrow: 1
};

const rnasList = {
	flexGrow: 1,
	overflow: 'auto',
	width: '100%',
	paddingTop: 0,
};

const newRnaButton = {
	width: '75%',
};

const styles = { rnasPage, rnasList, newRnaButton };

export default styles;
