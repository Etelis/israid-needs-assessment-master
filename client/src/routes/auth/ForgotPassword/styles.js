const container = {
  display: "flex",
  flexDirection: "column",
  alignItems: "center",
  maxWidth: "400px",
  margin: "0 auto",
  padding: "2rem",
};

const errorAlert = {
  marginBottom: "1rem",
};

const formInputText = {
  margin: "30px",
};

const submitButton = {
  width: "200px",
  backgroundColor: "#4496FC",
  margin: "0 auto",
};

const title = {
  textAlign: "center",
  marginTop: "20px",
  fontWeight: "bold",
};

const styles = {
  container,
  errorAlert,
  formInputText,
  submitButton,
  title,
};

export default styles;