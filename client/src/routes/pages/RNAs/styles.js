const rnasPage = {
	width: '100%',
	height: '90vh',
	padding: '14px',
	paddingBottom: 3,
	paddingTop: 0,
	alignItems: 'center',
	flexGrow: 1,
};

const filterControlsContainer = {
	display: 'flex',
	width: 1,
	alignItems: 'center',
	gap: '5px',
};

const searchFilter = {
	flex: 6,
};

const sort = {
	display: 'flex',
	flex: 1,
};

const rnasList = {
	flexGrow: 1,
	overflow: 'auto',
	width: '100%',
	paddingTop: 0,
};

const newRnaButton = {
	width: '75%',
	height: '50px',
};

const styles = {
	rnasPage,
	filterControlsContainer,
	searchFilter,
	sort,
	rnasList,
	newRnaButton,
};

export default styles;
